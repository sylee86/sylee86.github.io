import pagesTitle from "./../db/pagesTitle.json"; // eslint-disable-line no-unused-vars

function Header() {
  return (
    <header className="header">
      <h1>213123</h1>
    </header>
  );
}

export default Header;
